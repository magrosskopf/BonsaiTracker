# Sandcastle Setup Completion

**Status**: IMPLEMENTED  
**Created**: 2026-07-03  
**Last Modified**: 2026-07-03

## Purpose/Goal

Complete the generated Sandcastle setup so it matches this project's package manager, validation dependency, prompts, and coding standards.

## Functional Requirements

1. Keep the sandbox dependency strategy aligned with npm and the existing `node_modules` copy optimization.
2. Ensure a schema validator is installed for the planner `<plan>` output.
3. Customize `.sandcastle/` prompts so agents follow the Bonsai Tracker workflow, checks, and commit expectations.
4. Replace placeholder coding standards with Bonsai Tracker-specific standards.

## Technical Constraints

- Stack: Next.js Pages Router, TypeScript, Prisma, Tailwind.
- Package manager: npm with `package-lock.json`.
- Required checks: `npm test`, `npm run typecheck`, and `npm run build` when relevant.
- Do not change central `workflows/` files.

## Acceptance Criteria

- [x] `.sandcastle/main.mts` keeps npm-compatible sandbox setup.
- [x] `zod` is present in project dependencies.
- [x] Prompt files no longer contain irrelevant template assumptions.
- [x] `.sandcastle/CODING_STANDARDS.md` contains concrete project standards.
- [x] Sandcastle script placeholders are wired as far as local tooling permits.

## Out-of-Scope

- Changing application runtime behavior.
- Migrating package managers.
- Changing central workflow definitions.
- Creating or modifying GitHub issues.

## Dependencies

- Existing `.sandcastle/` template generated by `@ai-hero/sandcastle`.
- Existing npm dependency tree.

## Open Questions

- None.
