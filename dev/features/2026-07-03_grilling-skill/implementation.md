# Implementation Plan: Grilling Skill

**Status**: COMPLETE  
**Created**: 2026-07-03  
**Last Modified**: 2026-07-03

## Overview

Create a local Codex skill named `grilling` in the auto-discovered personal skills directory. The skill will preserve the user's supplied behavior: relentlessly question a plan or design, ask one question at a time, provide a recommended answer, inspect the codebase when possible, and avoid implementation until the user confirms shared understanding.

## Reference

- **Spec**: `dev/features/2026-07-03_grilling-skill/spec.md`
- **Acceptance Criteria**:
  - Skill folder exists at `${CODEX_HOME:-$HOME/.codex}/skills/grilling`.
  - `SKILL.md` has valid `name: grilling` frontmatter and trigger description.
  - Skill instructions enforce one question at a time, recommended answer, local exploration where possible, and no enactment before confirmation.
  - `agents/openai.yaml` exists.
  - Skill passes `quick_validate.py`.

## File Structure

### Files to Create

- `${CODEX_HOME:-$HOME/.codex}/skills/grilling/SKILL.md` - Main skill metadata and instructions.
- `${CODEX_HOME:-$HOME/.codex}/skills/grilling/agents/openai.yaml` - UI metadata generated by skill tooling.

### Files to Modify

- `dev/features/2026-07-03_grilling-skill/spec.md` - Mark implemented during DONE.
- `dev/features/2026-07-03_grilling-skill/implementation.md` - Mark complete during DONE.

## Implementation Steps

### Step 1: Resolve Paths and Tooling

**Goal**: Confirm the skill output path and creator script locations.

**Actions**:
1. Determine the personal skills root with `${CODEX_HOME:-$HOME/.codex}/skills`.
2. Locate `init_skill.py`, `quick_validate.py`, and `generate_openai_yaml.py` in the skill-creator directory.

**Files involved**: Skill-creator system directory.

### Step 2: Initialize Skill

**Goal**: Create the standard skill folder structure.

**Actions**:
1. Run `init_skill.py grilling --path "${CODEX_HOME:-$HOME/.codex}/skills"` with interface values for `display_name`, `short_description`, and `default_prompt`.
2. Do not request optional resource directories because this skill needs only instructions and metadata.

**Files involved**: `${CODEX_HOME:-$HOME/.codex}/skills/grilling/`.

### Step 3: Write Skill Instructions

**Goal**: Replace the generated template with the final concise `SKILL.md`.

**Actions**:
1. Use frontmatter containing only `name` and `description`.
2. Include the user's supplied operating rules.
3. Keep the body concise and imperative.

**Files involved**: `${CODEX_HOME:-$HOME/.codex}/skills/grilling/SKILL.md`.

### Step 4: Validate Skill

**Goal**: Verify the skill is structurally valid.

**Actions**:
1. Run `quick_validate.py "${CODEX_HOME:-$HOME/.codex}/skills/grilling"`.
2. Fix any reported frontmatter, naming, or metadata issues.

**Files involved**: `${CODEX_HOME:-$HOME/.codex}/skills/grilling/`.

### Step 5: Mark Documentation Complete

**Goal**: Close the workflow documents.

**Actions**:
1. Mark `spec.md` as `IMPLEMENTED`.
2. Mark `implementation.md` as `COMPLETE`.

**Files involved**: `dev/features/2026-07-03_grilling-skill/spec.md`, `dev/features/2026-07-03_grilling-skill/implementation.md`.

## Code Architecture

### Key Components

#### Skill Definition

- **Purpose**: Define trigger metadata and runtime behavior for Codex.
- **Location**: `${CODEX_HOME:-$HOME/.codex}/skills/grilling/SKILL.md`
- **Dependencies**: Codex skill loader.

#### Agent Metadata

- **Purpose**: Provide UI-facing display name, short description, and default prompt.
- **Location**: `${CODEX_HOME:-$HOME/.codex}/skills/grilling/agents/openai.yaml`
- **Dependencies**: Skill-creator generator.

### Data Models

No application data models are involved.

### Module Interactions

Codex skill discovery loads `SKILL.md` metadata, triggers the skill from the description, then loads the body instructions when the skill is selected.

## Technical Decisions

- **No resource directories**: The skill only needs procedural instructions, not scripts, references, or assets.
- **Personal skills path**: Use `${CODEX_HOME:-$HOME/.codex}/skills` for automatic discovery.
- **Tool-generated metadata**: Use skill-creator tooling for `agents/openai.yaml`.

## Integration Points

### Existing Code Integration

None. This does not modify Bonsai Tracker application code.

### Database Changes

None.

## Test Strategy

### Unit Tests

No project unit tests apply.

### Validation Tests

- Run `quick_validate.py` against the created skill folder.
- Inspect `SKILL.md` and `agents/openai.yaml` for compliance with acceptance criteria.

### Test Data

No test data required.

## Edge Cases & Error Handling

### Edge Cases

1. Existing `grilling` skill already exists: inspect it before editing and preserve unrelated user changes where possible.
2. `CODEX_HOME` unset: use `$HOME/.codex`.

### Error Scenarios

1. Skill-creator script cannot run: report the error and use the nearest equivalent manual structure only if needed.
2. Validation fails: fix reported issues and rerun validation.

## Performance Considerations

No runtime performance impact.

## Security Considerations

- Do not execute network operations.
- Do not include secrets or project-specific private data in the skill.

## Rollback Plan

If implementation fails:
1. Leave workflow docs with current status and report the failure.
2. Do not delete existing user-owned skill files without explicit approval.

## Validation Checklist

- [x] All planned files created or updated.
- [x] `SKILL.md` contains valid frontmatter.
- [x] `agents/openai.yaml` exists and matches the skill.
- [x] `quick_validate.py` passes.
- [x] Acceptance criteria from spec are met.
- [x] No application code changed.

## Notes

The user's desired `SKILL.md` content is already concise and should be preserved closely, with only minimal metadata wording improvements if needed for triggering.
